## 0.1.1（2026-09-08）

- 词典管理列表每本词典前增加浅色三横杠拖动手柄。
- 长按拖动手柄即可上下移动词典，使用 `ItemTouchHelper` 实现拖动排序。
- 排序结果持久化到 `filesDir/dicts_meta.json` 的 `order` 字段，重启后保留。
- 查询引擎读取启用词典的管理顺序，并在匹配等级相同的情况下按该顺序排序展示结果。
- 未启用词典继续由 `listEnabled()` 过滤，即使其优先级排在最前，也不会出现在查询结果中。
- 保留原有启用/停用、删除、导入及预热逻辑。

## 查询键盘交互

- 候选结果 `RecyclerView` 开始上下移动时自动隐藏输入法键盘。
- 输入框仍可继续编辑；点击候选词、清除和详情页面的原有键盘行为不变。
- 目的：滑动浏览候选结果时避免软键盘遮挡列表内容。

## 界面细节

- 词典管理页 Wi‑Fi 入口继续增加右侧内边距，降低贴近屏幕边缘导致的误触风险。
- 拖动手柄使用低对比度浅色，保持与词典卡片和现有 Material 3 界面一致。

## 兼容与验证

- 版本号：versionCode 4，versionName 0.1.1。
- 继续使用历史 release keystore，正式包可覆盖 v0.1.0。
- 现有词典元数据中没有 `order` 时使用兼容默认顺序，并在用户排序后写入新字段。
- 待用户确认对外日志后，使用 GitHub Actions 在北京时间 11:00（UTC 03:00）构建并发布正式 APK。
