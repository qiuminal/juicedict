## 0.1.3（2026-09-13）

### 查询历史界面

- 修复点击「睁眼/闭眼」隐藏查询历史时，整个查询历史模块在纵向上异常上下移动的问题。
- 根因一：历史标题行原先为 `wrap_content`，行内清空按钮高 36dp、眼睛按钮高 32dp；隐藏历史时清空按钮被置为 `GONE`，标题行高度由 36dp 塌陷为 32dp，导致标题及以下整块内容上移。修复方式：将标题行高度固定为 36dp，行高不再随任何子控件显隐变化。
- 根因二：历史容器与结果容器原先在 `contentRoot` 中同时持有 `layout_weight="1"`，隐藏历史容器会使其权重被剔除，导致父级重新分配剩余高度。修复方式：结果容器改用 `match_parent`，不再与历史容器争抢权重。
- 修复后，历史模块显示期间其权重与高度完全不变，睁眼/闭眼仅切换历史卡片内容的可见性，不触发父容器重新测量分配。
- 查询历史瀑布流占满搜索框下方剩余空间，超出部分可滚动查看完整历史卡片。

### 工程与验证

- 版本号更新为 `versionCode = 6`、`versionName = "0.1.3"`。
- 本地验证：`testDebugUnitTest` 与 `assembleRelease` 通过。
- Release APK 使用历史正式签名证书验证通过。
- 真机验证：用户在已连接设备上确认历史隐藏切换不再出现上下移动。
- 对外精简日志见 `release-notes/v0.1.3.md`，与客户端「关于 → 更新日志」保持核心内容一致。

## 0.1.2（2026-09-09）

### 查询历史

- 新增查询历史功能：仅在用户点击候选词并打开词条详情后记录，不记录单纯输入、候选列表浏览或无结果查询。
- 历史记录持久化保存，最多保留最近 30 条；相同词条重新打开详情时移到最前并更新预览和出处。
- 历史条目点击后直达对应词条详情，并保留词典 ID、文章 offset/size，确保重启后仍能加载完整释义。
- 历史条目采用词条标题 + 词典名首行、释义预览后续行的卡片结构；释义最多展示四行。
- 历史卡片改为双列瀑布流：左右两列独立排列，短卡片下方不为另一列的长卡片保留空白。
- 历史文字采用低饱和度中性色；词典出处使用低饱和浅紫色，与正式查询结果的主题强调色区隔；卡片底色使用 Material 3 主题容器色。
- 查询历史标题旁增加睁眼/闭眼切换：隐藏状态仅隐藏历史条目和清空按钮，标题与闭眼按钮保持可见，状态持久化。

### 查询与返回交互

- 修复逐字输入新查询时候选列表滚动位置未重置的问题；每次输入和异步结果落地后均回到顶部，使逐字输入与一次性输入的首个候选一致。
- 从历史卡片进入详情时，搜索框保持焦点，光标位于词条末尾，清除按钮保持可用，但不自动弹出软键盘。
- 从历史详情使用 Android 返回键或全面屏返回手势时，清空搜索框、清空候选列表并回到首页，避免详情消失后残留无法继续查询的文本。
- 点击搜索框清除按钮后，自动聚焦并拉起键盘，方便立即输入新词条。

### 工程与验证

- 继续兼容 Android API 24+、Material 3 动态主题及深色模式。
- 沿用 v0.1.1 的词典优先级排序、禁用词典过滤、Wi-Fi 传词典和大词典稳定性改进。
- 本地验证：`testDebugUnitTest` 与 `assembleRelease` 通过。
- Release APK 使用历史正式签名证书验证通过。
- 本版本对外日志草案见 `release-notes/v0.1.2.md`，已按用户确认文案同步；待提交后使用 GitHub Actions 在北京时间 12:00（UTC 04:00）构建并发布。

## 0.1.1（2026-09-08）

### 词典优先级

- 词典管理列表每本词典前增加浅色三横杠拖动手柄。
- 长按拖动手柄即可上下移动词典，使用 `ItemTouchHelper` 实现拖动排序。
- 排序结果持久化到 `filesDir/dicts_meta.json` 的 `order` 字段，重启后保留。
- 查询引擎读取启用词典的管理顺序，并在匹配等级相同的情况下按该顺序排序展示结果。
- 未启用词典继续由 `listEnabled()` 过滤，即使其优先级排在最前，也不会出现在查询结果中。
- 保留原有启用/停用、删除、导入及预热逻辑。

### 查询键盘交互

- 候选结果 `RecyclerView` 开始上下移动时自动隐藏输入法键盘。
- 输入框仍可继续编辑；点击候选词、清除和详情页面的原有键盘行为不变。
- 目的：滑动浏览候选结果时避免软键盘遮挡列表内容。

### 界面细节

- 词典管理页 Wi-Fi 入口继续增加右侧内边距，降低贴近屏幕边缘导致的误触风险。
- 拖动手柄使用低对比度浅色，保持与词典卡片和现有 Material 3 界面一致。

### 兼容与验证

- 版本号：versionCode 4，versionName 0.1.1。
- 继续使用历史 release keystore，正式包可覆盖 v0.1.0。
- 现有词典元数据中没有 `order` 时使用兼容默认顺序，并在用户排序后写入新字段。
- 使用 GitHub Actions 云端构建并发布正式 APK，历史签名证书校验通过。
