# JuiceDict 内部详细更新日志

本文件用于开发追溯、维护和问题定位，记录每个版本的完整技术变更。它是**对内版**更新日志，不直接展示给普通用户。

客户端“关于 → 更新日志”以及 GitHub Release 页面使用简短的**对外版**更新日志。对外措辞须经用户确认；两版版本号和发布日期必须一致，但详细程度可以不同。

## 0.1.0（2026-09-08）

### Wi‑Fi 传词典

- 词典管理页右上角新增 Wi‑Fi 入口，进入“从电脑中导入”页面后启动内嵌 HTTP 服务。
- 手机显示局域网访问地址，电脑端只需浏览器，无需安装客户端。
- 浏览器支持拖拽松散 StarDict 文件或整个文件夹，也支持一次拖入多部词典。
- 支持 `.ifo`、`.idx`、`.idx.gz`、`.dict`、`.dict.dz` 和可选 `.syn` 文件。
- 按“同目录 + 同名词干”对文件分组；凑齐一部词典后自动导入。
- 上传采用流式 `PUT /upload`，不缓存整个请求体；支持进度反馈和并发上传。
- 电脑端显示文件进度及导入结果，手机端同步显示传输记录。
- 传输由 `dataSync` 前台服务保活，使用 WifiLock，并仅在上传期间持有 WakeLock；可从页面或通知栏停止。
- 默认使用 7800 端口，端口被占用时自动使用可用端口。
- 文件夹拖拽遍历复用同一个 Chromium directory reader，完整读取 `readEntries()` 的所有批次；增加读取中、空目录和读取失败反馈。
- 路径和文件安全校验：拒绝绝对路径、路径穿越、隐藏文件和超深路径；单文件上限 2GB；过滤 macOS 垃圾文件。

### 词典安装与存储可靠性

- SAF 导入和 Wi‑Fi 导入共用统一安装通路。
- 先写入 `.incoming-<base>` 临时目录，完成后原子替换，避免中断时留下半部词典。
- 重导入同名词典前清除已加载实例，避免继续查询旧数据。
- 暂存文件保留至 `/finish`，文件到达顺序不影响分组；晚到的 `.syn` 也能纳入最终安装。

### 大型词典稳定性

- 重写 `StarDictIndex` 内存布局：所有词头存入连续 `CharArray`，边界、偏移、长度和搜索序使用原始类型数组。
- 索引比较直接操作字符区域，避免为每次比较构造 String。
- 使用无装箱的稳定归并排序，替代百万级对象集合排序。
- `.idx` / `.idx.gz` 解析与 `.jidx` 缓存读写改为流式处理，降低峰值内存占用。
- 340 万词条 ECDICT 的常驻索引内存由旧实现的 500MB 以上降至约 190MB。
- 开启 `largeHeap`，为大型词典保留足够堆空间。
- `DictionaryRepository.open/article/prewarmAll`、应用后台协程及跨词典查询增加异常隔离；单部损坏或超限词典不再导致 App 进程闪退。
- 修复截断 `.idx` 的残词条字符污染上一词条的问题：仅在词头和偏移/长度全部读取完整后提交词条。

### 界面和交互

- 电脑端和 App 端统一为“显示器 + Wi‑Fi”传输图标。
- 词典管理页 Wi‑Fi 入口增加右侧内容边距，使图标远离屏幕边角、更容易点击。
- “从电脑中导入”页面电脑屏幕内直接引用 App 原版 launcher 图标，不再单独缩放前景书籍图案。

### 验证

- 77 个 JVM 测试全部通过，包括 Wi‑Fi 导入/HTTP 集成测试、紧凑索引回归测试和真实 340 万词条 ECDICT 全量测试。
- ECDICT 测试覆盖：全量解析与排序、精确/前缀查询、正文读取、`.jidx` 写出及回读一致性。
- 正式 APK 使用历史 release keystore 签名，可覆盖旧版本安装。
